[project]
name = "network_qa"
version = "0.1.0"
description = "Quality-assurance metrics + decisions for the r01network pipeline (imported by network_fmri and other orchestrators)."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
# nibabel/numpy are imported only by the optional sibling integration module.
dev = ["pytest>=8", "nibabel>=5", "numpy"]

[project.scripts]
network-qa = "network_qa.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/network_qa"]

[tool.pytest.ini_options]
testpaths = ["tests"]
