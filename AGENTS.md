# Project agent memory

- Read `README.md` for the two-stage exclusion contract and scientific defaults;
  `CONTRIBUTING.md` owns setup and coordinated dependency publication.
- Use `uv run --frozen pytest -q`. `docs/CODE-REVIEW.md` records audit coverage,
  deliberate missing-evidence behavior, and the optional real GLM/fMRI integration test.
- Keep QA orchestration-free. Scientific exclusion/performance-policy changes need
  a separate study decision; input-validation fixes must preserve the documented defaults.

## Maintaining this file

Keep this file for knowledge useful to almost every future agent session in this project.
Do not repeat what the codebase already shows; point to the authoritative file or command instead.
Prefer rewriting or pruning existing entries over appending new ones.
When updating this file, preserve this bar for all agents and keep entries concise.
